# hello.py — our first file in the github_lab project
def greet(name):
    """Return a friendly greeting."""
    return f"Hello, {name}! Welcome to the Git lab."

if name == "main":
    print(greet("Student"))